# Project requirements
# PyTorch >= 1.7 (CPU or GPU). GPU / MPS (M1 Mac) recommended for speed.
# Install with:  pip install -r requirements.txt
torch>=1.7
torchvision>=0.8
opencv-python>=4.5
numpy>=1.21
PyYAML>=5.3
requests>=2.25
fastapi>=0.100
uvicorn>=0.20
python-multipart>=0.0.5
matplotlib>=3.4
scipy>=1.7
seaborn>=0.11
tqdm>=4.60
pandas>=1.3

# On Apple Silicon (M1/M2) Macs use the Apple-optimized builds:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# (or use conda/mamba for MPS support)
